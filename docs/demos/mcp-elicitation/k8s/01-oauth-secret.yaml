---
# OAuth Secret for MCP Elicitation Demo
#
# NOTE: This file is now a TEMPLATE and should NOT be applied directly.
# The actual secret is created dynamically by the deploy.sh script using
# credentials from the .env file.
#
# This template is kept for reference and documentation purposes only.
#
# To create the secret manually (not recommended):
#   kubectl create secret generic mcp-elicitation-oauth-secret \
#     --from-literal=client-id=your-client-id \
#     --from-literal=client-secret=your-client-secret \
#     --namespace=kagenti-mcp-elicitation
#
# The deploy.sh script will automatically:
# 1. Load credentials from .env file
# 2. Delete any existing secret
# 3. Create a new secret with the loaded credentials

# TEMPLATE - DO NOT APPLY DIRECTLY
# apiVersion: v1
# kind: Secret
# metadata:
#   name: mcp-elicitation-oauth-secret
#   namespace: kagenti-mcp-elicitation
#   labels:
#     app.kubernetes.io/name: mcp-elicitation-oauth-secret
#     app.kubernetes.io/component: auth
#     app.kubernetes.io/part-of: kagenti
# type: Opaque
# stringData:
#   # OAuth Client ID - loaded from .env
#   client-id: "LOADED_FROM_ENV"
#
#   # OAuth Client Secret - loaded from .env
#   client-secret: "LOADED_FROM_ENV"

# Made with Bob

