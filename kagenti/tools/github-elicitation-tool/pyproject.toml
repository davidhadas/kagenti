[project]
name = "github-elicitation-tool"
version = "0.1.0"
description = "Small MCP proxy tool for the MCP elicitation demo"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "httpx>=0.27.0",
    "uvicorn[standard]>=0.34.0",
]

[tool.uv]
package = false